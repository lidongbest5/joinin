import {CALL_API} from 'redux-api-middleware';
import * as Constants from '../constants';

export function resetEvent(data) {
  return {
    type: Constants.RESET_EVENT,
  };
}

export function resetEvent1(data) {
  return {
    type: Constants.RESET_EVENT,
  };
}
